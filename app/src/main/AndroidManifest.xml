<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/dblogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dblogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppExamen"
        tools:targetApi="31">

        <!-- Main Activity (DragonballActivity) -->
        <activity
            android:name=".frameworks.views.activities.DragonballActivity"
            android:exported="true"
            android:theme="@style/Theme.AppExamen.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
